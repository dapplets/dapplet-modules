{
  "name": "@dapplets/common-lib",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run copymanifest && cross-env-shell \"browserify -e src/index.ts -p tsify -o ../../dist/$npm_package_dapplets_name/$npm_package_dapplets_branch/$npm_package_version/index.js\"",
    "watch": "npm run copymanifest && cross-env-shell \"watchify -v -e src/index.ts -p tsify -o ../../dist/$npm_package_dapplets_name/$npm_package_dapplets_branch/$npm_package_version/index.js\"",
    "clean": "(if exist lib rd /q /s lib) & (if exist dist rd /q /s dist)",
    "copymanifest": "cross-env-shell \"copyfiles manifest.json ../../dist/$npm_package_dapplets_name/$npm_package_dapplets_branch/$npm_package_version\""
  },
  "author": "Dapplets Team",
  "license": "MIT",
  "devDependencies": {
    "@dapplets/dapplet-extension-types": "0.1.0"
  },
  "dapplets": {
    "name": "common-lib.dapplet-base.eth",
    "branch": "default"
  }
}
