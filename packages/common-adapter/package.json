{
  "name": "common-adapter.dapplet-base.eth",
  "version": "0.3.6",
  "description": "Generic adapter for all sites",
  "scripts": {
    "build": "webpack --config webpack.dev.js",
    "watch": "webpack -w --config webpack.dev.js",
    "clean": "rimraf build lib"
  },
  "author": "Dapplets Project",
  "license": "MIT",
  "main": "build/index.js",
  "typings": "build/index.d.ts",
  "devDependencies": {
    "dynamic-adapter.dapplet-base.eth": "^0.6.0"
  }
}
