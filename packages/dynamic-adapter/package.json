{
  "name": "@dapplets/dynamic-adapter",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build": "webpack --config webpack.dev.js",
    "watch": "webpack -w --config webpack.dev.js",
    "clean": "(if exist build rd /q /s build) & (if exist dist rd /q /s dist)"
  },
  "author": "Dapplets Team",
  "license": "MIT",
  "directories": {
    "build": "build"
  },
  "files": [
    "build"
  ],
  "main": "build/index.js",
  "typings": "build/index.d.ts",
  "devDependencies": {
    "@dapplets/dapplet-extension-types": "^0.3.12"
  }
}
