{
  "name": "@dapplets/<%= projectName %>",
  "version": "0.1.0",
  "description": "<%= description %>",
  "main": "lib/index.js",
  "scripts": {
    "build": "webpack --config webpack.dev.js",
    "watch": "webpack -w --config webpack.dev.js",
    "clean": "(if exist lib rd /q /s lib) & (if exist dist rd /q /s dist)"
  },
  "author": "<%= author %>",
  "license": "MIT",
  "devDependencies": {
    "@dapplets/dapplet-extension": "^0.1.4",
    "@dapplets/twitter-adapter": "^0.3.3"
  }
}
